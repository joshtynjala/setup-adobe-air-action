name: Setup Adobe AIR SDK
author: Josh Tynjala
description: Action to setup Adobe AIR SDK

inputs:
  air-version:
    description: "Version of the Adobe AIR SDK (example: 32.0)"
    required: false
  accept-license:
    description: "Accept the Adobe AIR SDK License Agreement (example: true)"
    required: false

runs:
  using: "node12"
  main: "main.js"

branding:
  color: "blue"
  icon: "play"
